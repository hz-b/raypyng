version: 2

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.12"
  jobs:
    pre_build:
      - python docs/prepare_example_assets.py

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: requirements.txt
